class ProgramaCorreto {
    int x, y;
    float z;
    string nome;
    {
        x := 10;
        y := x + 5;
        z := x / 2.0;
        if (x > y) {
            x := y;
        } else {
            y := x;
        };
        do {
            x := x - 1;
        } while (x > 0);
        repeat {
            y := y + 1;
        } until (y >= 10);
        read(nome);
        write("Resultado: " + nome);
    }
}
